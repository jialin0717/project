# 项目目标：代码故障分析工具 (Fault Analyzer)
## 功能需求
1. 使用 `requests` 库读取一段包含错误信息的模拟日志 (Mock Logs)。
2. 使用 `pandas` 库分析日志，统计不同错误类型（如 SyntaxError, Runtime/ValueError）出现的频率。
3. 调用 AI 接口（或模拟调用）对最高频的错误给出修复建议。
## 团队角色
- 成员 A : 功能开发。
- AI Agent: 代码生成与错误修正。

